# MikuPlay Reburn 内置功能移植工作记录

## 项目基本信息

- **项目名称**：MikuPlay Reburn 宿主源码内置功能移植
- **工作目录**：`/home/user/Doubao/chats/31152236319/MikuPlayReburn/`
- **开始时间**：2026-09-22
- **当前版本**：Web 构建通过，待用户在电脑端执行 Android APK 构建

---

## 一、已完成功能模块清单

### 1. Tab 导航栏改造
- **文件**：`src/styles/components/navBar.css.ts`、`src/UIComponents/featurePanels/NavBar.ts`
- **功能**：
  - 将 `.nav-items-container` 从 `overflow-x: auto` 改为 `flex-wrap: wrap`
  - `.nav-item` 高度固定 48px
  - Tab 超出屏幕宽度自动换行，不再横向滑动
- **Tab 顺序**：导入 → 环境 → 模型 → 着色 → 内容库 → 后处理 → 灯光 → 导演模式 → 杂项

### 2. 灯光管理器内置
- **来源插件**：灯光管理器V3.2.3.mkp（3525 行 JS）
- **文件**：
  - `src/UIComponents/featurePanels/lights/lightMgrCore.js`（核心逻辑）
  - `src/UIComponents/featurePanels/lights/index.ts`（TS 包装器）
- **功能**：
  - 4 种灯光类型：环境光、方向光、点光灯、聚光灯
  - 灯光分组管理
  - 骨骼追踪灯
  - storage 走内容库 API（双层存储：localStorage + 内容库）
  - 新增导出/导入预设到内容库按钮

### 3. 模型移动工具合并
- **来源插件**：模型移动工具v1.0.0.mkp（514 行）
- **文件**：
  - `src/UIComponents/featurePanels/model/GizmoOperationSection.ts`
- **功能**：
  - 集成 Babylon.js GizmoManager
  - 移动/旋转/缩放三种操作模式
  - 射线拾取只命中模型列表内 mesh

### 4. 面部相机集成
- **来源插件**：面部相机V1.0.mkp（3391 行）
- **文件**：
  - `src/UIComponents/featurePanels/faceCam/faceCamCore.js`
  - `src/UIComponents/featurePanels/faceCam/index.ts`
- **功能**：
  - 导出 `createFaceCameraModule()` 和 `createGazeFollowModule()` 两个工厂函数
  - 在 `CameraSection.ts` 中改为二选一切换按钮：导入镜头文件 / 打开面部相机
  - 面部相机默认折叠操作面板，点击展开

### 5. VMD API 服务
- **文件**：`src/features/mmd/VmdApiService.ts`
- **功能**：全局单例，10 个接口，供插件读写 VMD 动作文件

### 6. 粒子系统 API 服务
- **文件**：`src/features/particle/ParticleApiService.ts`
- **功能**：全局单例，11 个接口，供插件注册/替换/执行粒子效果

### 7. 内容库系统
- **文件**：
  - `src/features/library/ContentLibraryApi.ts`（全局单例）
  - `src/UIComponents/featurePanels/contentLibrary/index.ts`
- **功能**：
  - 统一文件管理器 UI
  - 左侧分类文件夹栏 + 右侧文件表格
  - 顶部工具栏：搜索/导入/一键整理
  - 分类：models / materials / textures / music / images / lights / projects / shaders
  - 所有文件统一放在手机文件管理器 MikuPlay 文件夹下
  - 支持压缩包自动解压
  - 允许外部文件管理器（MT管理器）直接整理

### 8. 自动存档服务
- **文件**：`src/features/project/AutoSaveService.ts`
- **功能**：
  - 三档间隔：关闭 / 5分钟 / 10分钟
  - 开始渲染视频时立即自动保存一份临时存档
  - 手动存档/读取
  - 存档文件无需加密
  - 自动同步到内容库 projects 分类

### 9. 导演模式移植
- **来源插件**：导演模式1.0.mkp（200 行）
- **文件**：
  - `src/UIComponents/featurePanels/director/directorCore.ts`（类型定义+工具函数）
  - `src/UIComponents/featurePanels/director/index.ts`（UI 面板）
- **功能**：
  - 镜头节点系统：创建/编辑/删除/排序镜头关键帧
  - 7 种预设镜头：推进/拉远/环绕/升降/平移/甩镜/追踪
  - 4 种插值曲线：线性/正弦/指数/缓入缓出
  - 全局参数同步：统一起始间隔/运动时长/结束间隔/曲线
  - 用户预设组：最多 10 组，支持替换/追加
  - 镜头接管：拍摄模式下按节点顺序自动执行
  - 时长约束：常规模式（受动作总帧数约束）/ 自由模式
  - 导出/导入预设到内容库按钮

### 10. 工程存档系统增强
- **文件**：
  - `src/features/project/ProjectArchive.ts`
  - `src/features/project/ProjectCollector.ts`
  - `src/features/project/ProjectRestorer.ts`
  - `src/features/project/ProjectSaveManager.ts`
- **存档内容完整性**：
  - ✅ 场景环境（背景/地面/灯光阴影）
  - ✅ 模型与关联动作
  - ✅ 着色器材质参数
  - ✅ 后处理参数
  - ✅ 模型操作（骨骼校正/绑定/表情/变换）
  - ✅ 粒子系统
  - ✅ 物理引擎设置
  - ✅ 相机偏移
  - ✅ 音乐轨道列表
  - ✅ 应用设置
  - ✅ 灯光管理器预设（新增）
  - ✅ 导演模式镜头节点与预设（新增）
- **存档格式**：`.mikuproj`（单个 JSON 文件）

### 11. 其他 API 服务
- `src/features/mmd/BoneApiService.ts` — 骨骼操作
- `src/features/mmd/MorphApiService.ts` — 表情变形
- `src/features/render/ScreenshotApiService.ts` — 截图
- `src/features/ui/ShortcutApiService.ts` — 快捷键
- `src/features/ui/NotificationApiService.ts` — 通知
- `src/features/settings/SettingsApiService.ts` — 设置持久化

### 12. API 规范文档
- **文件**：`插件开放API文档.md`（33 章，1208 行）
- **内容**：
  - 插件运行环境（PluginContext/EventBus/Storage）
  - 宿主扩展对象 mp（UI/File/Model/ParticleRegistry/MaterialRegistry）
  - 核心子系统 API（骨骼/表情/相机/灯光/物理/后处理/场景/动画）
  - 内容库/工程存档/截图/快捷键/通知/设置 API
  - 适配器接口规范（IMaterialAdapter/IPostProcessAdapter/ControlDeclaration）
  - Babylon.js 安全白名单
  - 安全约束机制

---

## 二、已修改的宿主源码文件清单

| 文件路径 | 修改内容 |
|---------|---------|
| `src/styles/components/navBar.css.ts` | Tab 自动换行 |
| `src/UIComponents/featurePanels/NavBar.ts` | 注册 lights/library/faceCam/director 等 Tab |
| `src/UIComponents/featurePanels/model/ModelOptPanel.ts` | 集成 GizmoOperationSection |
| `src/UIComponents/featurePanels/import/CameraSection.ts` | 二选一切换按钮 |
| `src/features/render/OfflineRenderManager.ts` | 渲染前自动保存钩子 |

---

## 三、新建文件清单

### UI 面板
- `src/UIComponents/featurePanels/model/GizmoOperationSection.ts`
- `src/UIComponents/featurePanels/lights/lightMgrCore.js`
- `src/UIComponents/featurePanels/lights/index.ts`
- `src/UIComponents/featurePanels/faceCam/faceCamCore.js`
- `src/UIComponents/featurePanels/faceCam/index.ts`
- `src/UIComponents/featurePanels/contentLibrary/index.ts`
- `src/UIComponents/featurePanels/director/directorCore.ts`
- `src/UIComponents/featurePanels/director/index.ts`

### 功能服务
- `src/features/mmd/VmdApiService.ts`
- `src/features/mmd/BoneApiService.ts`
- `src/features/mmd/MorphApiService.ts`
- `src/features/particle/ParticleApiService.ts`
- `src/features/library/ContentLibraryApi.ts`
- `src/features/project/AutoSaveService.ts`
- `src/features/render/ScreenshotApiService.ts`
- `src/features/ui/ShortcutApiService.ts`
- `src/features/ui/NotificationApiService.ts`
- `src/features/settings/SettingsApiService.ts`

### 文档
- `插件开发规范.md`
- `插件开放API文档.md`（33 章）
- `工作记录.md`（本文件）

---

## 四、用户明确要求的功能点

1. ✅ 文件统一放在手机文件管理器的 MikuPlay 文件夹下
2. ✅ 压缩包以压缩包形式保存，读取时解压
3. ✅ 允许用户通过外部文件管理器（MT管理器等）整理
4. ✅ 开始渲染视频时立即自动保存一份临时存档
5. ✅ 自动存档间隔可选 5/10 分钟
6. ✅ 存档文件无需加密
7. ✅ 所有新增模块的导入导出都要与内容库正确关联通信
8. ✅ Tab 超出屏幕范围自动换行
9. ✅ 面部相机与镜头导入二选一切换
10. ✅ MME 材质库作为独立插件可摒弃，整合到内容库

---

## 五、用户明确拒绝的方案

1. ❌ Glare v2.x 多槽独立金字塔链（持续黑屏，已放弃）
2. ❌ 插件加载机制（改为直接内置到宿主源码）
3. ❌ MME 材质库作为独立插件（用户说"可以摒弃"）

---

## 六、性能与内存评估

### 已验证安全的点
- 导演模式渲染循环：`onBeforeRenderObservable.add()` 在停止拍摄时正确移除
- 灯光管理器渲染循环：`animFrameObserver` 正确移除
- 所有灯光/阴影生成器都有 dispose 逻辑
- 反射地面纹理资源正确释放
- 动画结束检查观察者正确移除

### 潜在风险点（低）
- 全局单例 260 个（大部分是 Babylon.js 引擎内部单例）
- 灯光管理器 JS 中有大量 DOM 事件（面板关闭时 GC 自动回收）
- 内容库文件列表缓存（可按需优化）

### 构建状态
- ✅ Web 构建通过（约 21 秒）
- ✅ TypeScript 类型检查通过
- ✅ 无致命错误

---

## 七、未完成 / 待后续优化项

1. **MME Ray材质库（M2a1.0）和MME Ray渲（M2b1.0）** — 尚未移植到宿主源码
2. **Glare 后处理插件** — 尚未移植到宿主源码
3. **软件内部文件读写全部切换到内容库 API** — 目前还是部分直接读文件路径
4. **渲染完成的视频自动保存到内容库** — saveRenderedVideo 已在 API 中定义，但未在渲染完成回调中调用
5. **面部相机的骨骼选择绑定 UI** — 只做了顶部二选一切换按钮，详细绑定 UI 待完善
6. **存档格式改为 .zip** — 当前是单个 JSON (.mikuproj)，如需标准 zip 需引入 JSZip

---

## 八、下一步操作指引（用户端）

### 构建 APK
1. 把 `MikuPlayReburn-完整源码.zip` 复制到电脑
2. 解压到任意目录
3. 安装 Node.js 22+ 和 Android Studio（含 Android SDK）
4. 执行 `npm install`
5. 配置 Android SDK 环境变量（或创建 `local.properties`）
6. 执行 `npm run build` 先构建 Web 资源
7. 执行 `npm run build:android` 构建 APK
8. APK 输出路径：`android/app/build/outputs/apk/debug/app-debug.apk`
9. 传到手机安装测试

---

## 九、关键技术决策记录

1. **直接内置而非插件加载**：用户明确要求把插件代码直接移植到宿主源码中，而不是通过插件加载机制
2. **内容库作为统一文件入口**：所有文件读写都通过内容库 API，统一管理
3. **localStorage 作为快速缓存**：双层存储，localStorage 快速读写，内容库持久化
4. **全局单例模式**：所有 API 服务都用单例，避免重复初始化
5. **Tab 自动换行**：解决功能增多后 Tab 横向滑动的问题

---

**记录时间**：2026-09-22
**记录人**：Doubao
