# MikuPlay Reburn 编译指南

## 一、前置准备

### 1.1 硬件要求
- **CPU**：4 核以上，支持 x86_64
- **内存**：8GB 以上（建议 16GB，原生编译很吃内存）
- **磁盘空间**：至少 20GB 可用空间（SDK + NDK + 依赖缓存）
- **网络**：需要稳定的网络连接（下载 Gradle 依赖）

### 1.2 操作系统
- Windows 10/11（推荐）
- macOS 12+
- Linux Ubuntu 20.04+

---

## 二、软件安装清单

### 2.1 必须安装的软件

| 软件 | 版本要求 | 下载地址 | 用途 |
|------|---------|---------|------|
| **Git** | 最新版 | https://git-scm.com/downloads | 克隆/管理源码 |
| **Node.js** | 22.x LTS | https://nodejs.org/ | 前端构建 |
| **JDK** | 17 或 21 | https://adoptium.net/ | Gradle 编译 |
| **Android Studio** | Hedgehog (2023.1)+ | https://developer.android.com/studio | SDK + NDK + CMake |
| **Python** | 3.8+ | 系统自带或 https://python.org | 生成二维码头文件 |

### 2.2 安装步骤

#### Step 1：安装 JDK 17

**Windows**：
```bash
# 推荐使用 Chocolatey 安装
choco install temurin17

# 或手动下载安装后设置环境变量
# JAVA_HOME = C:\Program Files\Eclipse Adoptium\jdk-17.x.x-hotspot
```

**macOS**：
```bash
brew install --cask temurin@17
```

**验证**：
```bash
java -version
# 应显示 17.x.x
```

---

#### Step 2：安装 Node.js 22

**Windows/macOS**：
- 从官网下载 LTS 版本安装包
- 安装时勾选 "Add to PATH"

**Linux**：
```bash
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt install -y nodejs
```

**验证**：
```bash
node -v
# 应显示 v22.x.x

npm -v
# 应显示 10.x.x
```

---

#### Step 3：安装 Android Studio 与 SDK

1. 下载并安装 Android Studio
2. 首次启动会自动下载 SDK，等待完成
3. 打开 **SDK Manager**：
   - **SDK Platforms** 标签：
     - 勾选 **Android 14 (API 36)**（或 compileSdk 36）
   - **SDK Tools** 标签：
     - 勾选 **Android SDK Build-Tools** 最新版
     - 勾选 **Android SDK Platform-Tools**
     - 勾选 **Android SDK Tools** (Obsolete)
     - 勾选 **NDK (Side by side)** — 选择 r25c 或更高版本
     - 勾选 **CMake** — 选择 3.22.1 或更高版本
     - 点击 Apply 下载安装

**SDK 默认安装路径**：
- Windows：`C:\Users\你的用户名\AppData\Local\Android\Sdk`
- macOS：`~/Library/Android/sdk`
- Linux：`~/Android/Sdk`

---

#### Step 4：设置环境变量

**Windows**（系统属性 → 环境变量）：
```
JAVA_HOME = C:\Program Files\Eclipse Adoptium\jdk-17.x.x-hotspot
ANDROID_HOME = C:\Users\你的用户名\AppData\Local\Android\Sdk
ANDROID_SDK_ROOT = %ANDROID_HOME%
PATH 追加：
  %JAVA_HOME%\bin
  %ANDROID_HOME%\platform-tools
  %ANDROID_HOME%\emulator
```

**macOS/Linux**（写入 `~/.zshrc` 或 `~/.bashrc`）：
```bash
export JAVA_HOME=$(/usr/libexec/java_home -v 17)
export ANDROID_HOME=$HOME/Library/Android/sdk
export ANDROID_SDK_ROOT=$ANDROID_HOME
export PATH=$PATH:$ANDROID_HOME/platform-tools
```

**验证**：
```bash
echo $ANDROID_HOME
echo $JAVA_HOME
adb --version
```

---

## 三、源码准备

### 3.1 解压源码

```bash
# 解压源码包
unzip MikuPlayReburn-完整源码.zip
cd MikuPlayReburn
```

### 3.2 创建 local.properties

在项目根目录创建 `local.properties`，指向你的 SDK 路径：

**Windows**：
```properties
sdk.dir=C:\\Users\\你的用户名\\AppData\\Local\\Android\\Sdk
```

**macOS/Linux**：
```properties
sdk.dir=/Users/你的用户名/Library/Android/sdk
```

---

## 四、依赖安装

### 4.1 安装前端依赖

```bash
# 在项目根目录执行
npm install
```

**说明**：
- 约 300+ 个包，下载时间取决于网络
- 自动执行 `postinstall` 脚本（patch-package）
- 如果遇到 node-gyp 错误，先安装 Windows Build Tools：
  ```bash
  npm install --global windows-build-tools
  ```

### 4.2 验证 patch-package

```bash
ls patches/
# 应该看到 babylon-mmd 的 patch 文件
```

---

## 五、构建步骤

### 5.1 第一步：构建 Web 资源

```bash
npm run build
```

**预期结果**：
- 在 `dist/` 目录生成静态资源
- 构建时间约 20-30 秒
- 无报错即成功

---

### 5.2 第二步：同步 Capacitor

```bash
npx cap sync android
```

**作用**：
- 把 `dist/` 内容复制到 `android/app/src/main/assets/public/`
- 同步原生插件依赖

---

### 5.3 第三步：构建 Debug APK

```bash
cd android
./gradlew assembleDebug
```

**Windows**：
```cmd
cd android
gradlew.bat assembleDebug
```

**首次构建说明**：
- 第一次会下载 Gradle 和所有依赖，约 5-15 分钟
- 然后编译原生 C++ 代码（mbedTLS + curl + 主程序），约 10-20 分钟
- **总共约 15-30 分钟**，取决于电脑性能

**输出路径**：
```
android/app/build/outputs/apk/debug/app-debug.apk
```

---

### 5.4 第四步（可选）：构建 Release APK

```bash
cd android
./gradlew assembleRelease
```

**说明**：
- Release 版本会开启 R8 代码混淆和资源压缩
- 需要签名配置，否则会生成未签名 APK
- 用于正式发布

---

## 六、常见问题与解决

### 问题 1：NDK 版本不匹配

**错误**：`No toolchains found in the NDK toolchains folder for ABI with prefix: llvm`

**解决**：
1. 打开 Android Studio → SDK Manager → SDK Tools
2. 勾选 **Show Package Details**
3. 安装 **NDK r25c** 或 **r26b**
4. 或在 `android/app/build.gradle` 中指定 NDK 版本

---

### 问题 2：CMake 找不到

**错误**：`CMake not found`

**解决**：
1. SDK Manager → SDK Tools → 勾选 CMake
2. 或在 `local.properties` 中添加：
   ```properties
   cmake.dir=C\:\\Users\\xxx\\AppData\\Local\\Android\\Sdk\\cmake\\3.22.1
   ```

---

### 问题 3：Python 找不到 / qrcode 报错

**错误**：`'python' 不是内部或外部命令`

**解决**：
1. 确保 Python 已安装并加入 PATH
2. 或修改 `android/app/src/main/cpp/CMakeLists.txt`，把 `python` 改成 `python3`

---

### 问题 4：内存不足 / OOM

**错误**：编译过程中 Gradle 崩溃

**解决**：
修改 `android/gradle.properties`：
```properties
org.jvmargs=-Xmx4096m -XX:MaxPermSize=512m
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
```

---

### 问题 5：磁盘空间不足

**说明**：
- Android SDK：约 5GB
- NDK：约 2GB
- Gradle 缓存：约 2GB
- 项目构建产物：约 2GB
- **总计约 10-15GB**

**解决**：清理不需要的 SDK 版本和构建缓存

---

### 问题 6：网络下载慢 / Gradle 卡住

**解决**：配置国内镜像源

在 `~/.gradle/init.gradle` 中添加：
```groovy
allprojects {
    repositories {
        maven { url 'https://maven.aliyun.com/repository/google' }
        maven { url 'https://maven.aliyun.com/repository/public' }
        google()
        mavenCentral()
    }
}
```

---

## 七、安装与测试

### 7.1 安装到手机

```bash
# 方法 1：adb 直接安装
adb install android/app/build/outputs/apk/debug/app-debug.apk

# 方法 2：复制 APK 到手机手动安装
# 确保手机开启了"允许安装未知来源应用"
```

### 7.2 验证功能

安装后依次测试：
1. ✅ 软件能正常启动，不闪退
2. ✅ 能加载模型
3. ✅ 底部 Tab 栏：导入 → 环境 → 模型 → 着色 → 内容库 → 后处理 → 灯光 → 导演模式 → 杂项
4. ✅ 灯光面板能打开，4 种灯光可调
5. ✅ 内容库能打开，文件列表正常
6. ✅ 导演模式能打开，镜头节点正常
7. ✅ 面部相机能打开
8. ✅ 模型移动 Gizmo 正常

### 7.3 查看日志

```bash
# 实时查看日志
adb logcat | grep mikuplay

# 过滤崩溃日志
adb logcat *:E | grep AndroidRuntime
```

---

## 八、后续开发流程

### 8.1 修改前端代码后重新构建

```bash
npm run build
npx cap sync android
cd android && ./gradlew assembleDebug
```

### 8.2 使用 Android Studio 调试

```bash
# 在项目根目录执行
studio android
```

- 打开 Android 项目
- 连接手机后点击 Run
- 可以断点调试原生 C++ 和 Kotlin 代码

---

## 九、文件结构速查

```
MikuPlayReburn/
├── src/                          # 前端源码
│   ├── UIComponents/
│   │   ├── featurePanels/
│   │   │   ├── lights/           # 灯光管理器
│   │   │   ├── director/         # 导演模式
│   │   │   ├── faceCam/          # 面部相机
│   │   │   ├── contentLibrary/   # 内容库
│   │   │   └── model/
│   │   └── MainWindow.ts
│   └── features/                 # 功能服务
│       ├── mmd/
│       ├── particle/
│       ├── library/
│       └── project/
├── android/                      # Android 原生工程
│   ├── app/
│   │   └── src/main/
│   │       ├── cpp/              # C++ 原生代码
│   │       ├── java/             # Kotlin 代码
│   │       └── assets/           # Web 资源（构建后生成）
│   └── build.gradle
├── scripts/
│   └── generate_qrcode_header.py # 二维码头文件生成脚本
├── qrcode.jpg                    # 占位二维码（替换成真实的）
├── package.json
├── vite.config.ts
└── capacitor.config.json
```

---

**最后更新**：2026-09-22
